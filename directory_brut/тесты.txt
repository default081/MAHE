import urllib
def test1():
	File = open("m.txt", 'r') 
	for line in File:
		a = 'http://maulnet.ru/' + line
		url=urllib.urlopen(a)
		if url.code == 200:
			print a, url.code, "--Ditrctoty available"
			print "-------*--------"
		elif url.code == 404:
			print a, url.code ,"--error"
			print "-------*--------"

проверка первых 100 директорий прошла за 42 секунды 61 мили

---------------------------------------------------------------------------------------
